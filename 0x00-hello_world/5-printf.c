#include <stdio.h>
/**
 * main - A line of code that uses printf function
 * Return: This returns 0 as (successful)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
